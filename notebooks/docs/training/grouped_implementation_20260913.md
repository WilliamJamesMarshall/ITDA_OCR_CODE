# 병행 테스트·학습 실행 계획 반영 결과 (2026-09-13)

실행 진입점: `notebooks/project/run.py scripts.run_round_groups`.
현재 계획과 실제 명령은 [운영 안내](grouped_operations.md), 새 세션 지시는 [프롬프트](grouped_session_prompts.md)를 따른다.

## 구현·준비 완료

- 1 → 2·3 → 4·5 → 6·7 → 8, 총 5단계의 상태 조회 및 실행 제어.
- 3,716장의 새 회차 목록 생성. 중복 0, 누락 0. 초기 개발 사용은 1회 206장, 2회 499장, 3~8회 0장.
- 두 CPU 슬롯과 하위 프로세스 제한, 분리된 실행 사본·로그·후보, 회차별 대표 보고서.
- 최초 평가 보존, 사용자 피드백·명시적 승인과 원본 대응 증거 검사, 테스트 증강 파일 학습 편입 차단.
- 누적 원본 및 고정 그룹 분할, 회차별 후보와 별도 승인 통합 후보, 누적 회귀·실행·형식 검증과 명시적 선정.
- 새 작업 공간 `C:/ITDA_OCR_WORKSPACE/grouped-8-rounds-v2`에 기존 1회 테스트와 후속 학습·후보 기각 이력 연결.
- 완료된 최초 테스트·학습 worker는 증거 확인 후 재실행하지 않으며, 중단된 출력은 성공으로 간주하거나 덮어쓰지 않음.

## 검증

- 전체 자동 테스트 430개 통과 (12.793초). 승인·회차 분할·원본 crop·그룹 분할·후보 통합을 포함한 단위/fixture 검증.
- 실제 두 진단 프로세스와 각각의 자식 CPU affinity가 0–3 / 4–7임을 확인.
- PowerShell 실행 래퍼 구문 검사 및 `git diff --check` 통과.
- 실제 데이터 목록 `verify`, 현재 상태 `status`, 기존 이력 링크 해시 검사 통과.

이번 검증은 정식 OCR 병행 평가나 실제 optimizer 학습의 종단 간 성공을 뜻하지 않는다.
정식 오프라인 노트북 실행·메모리와 시간 측정·학습·export·통합 평가는 각 단계의 적절한 승인 후 수행한다.
공용 모델, 기존 테스트 결과 및 원본 이미지 파일은 이번 구현에서 교체하지 않았다.

## 확인 시점의 현재 상태

전체 5단계 중 1단계, 대상 1회차. 기존 최초 평가 및 후속 개발·학습 시도는 존재한다.
기존 개발 결과의 보호 정답 204/216을 연결했으나 새 단계의 최종 검증·선정 완료로 간주하지 않았다.
2~8회는 새 계획에서 미실행이다. 새 세션은 이 문서의 고정 시점 기록 대신 `status`를 다시 조회해야 한다.

구현 직후에는 원본 대응 미확정이 총 1,106건이었다.
이후 사용자 요청으로 [원본 대응 조사](derivative_resolution_20260913.md)를 실행해 1,084건을 연결했다.
남은 22건은 삭제 전 원본의 출처는 확인했지만 파일이 없는 상태이다. 위 문서와 현재 상태 조회가 최신 근거이다.
