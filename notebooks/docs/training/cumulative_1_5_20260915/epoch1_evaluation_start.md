# 1~5회 누적 1 epoch 후보 전체 이미지 평가 시작

2026-09-15 사용자가 직전 학습의 **1 epoch checkpoint 자체**로 1회 단독, 2·3회 병행, 4·5회 병행 테스트를 지시했다. 이전 테스트 보류를 이번 범위에서 해제한다. 내부 검증의 67/75 → 65/75 미통과 이력과 epoch 0 내부 선정은 변경하지 않는다. 이번 평가는 모델 채택이나 재학습 승인이 아니다.

- 실행 경로: `C:/ITDA_OCR_WORKSPACE/grouped-6-originals-v1/cumulative-1-5-epoch1-eval-20260915-v1`.
- checkpoint: `cumulative-1-5-20260915-v2/training-run/epoch_001.pdparams`.
- checkpoint SHA: `8ca6c7355fbdfbd12cd86d96898500e02a13f9d87c0ca2711c22d4eb89c00592`.
- 새 한국어 추론 parameters SHA: `dd4b4f14f53e702a224cedded5c898a771159d23861d60d0f25d53dd2b12cf15`.
- 원본은 전부 `C:/ITDA_OCR_CODE/학습대상데이터`에서 공급한다. 216/500/500/394/500장, 합계 2,110장이며 중복·누락·저장 증강 편입을 차단했다.
- 정답 XLSX SHA: `765db2f4781ec1975893e66b7c3c755f8a68246c68fc05d302597d36900a4d2d`. 881~1161 이동 및 1117=2021-06-26 정정을 반영한 기존 CSV를 그대로 고정했다.
- 직전 완료 보고서의 개선 추론 코드와 Notebook 내장 코드 일치를 확인했다. 실행기만 추가했고 새 추론 규칙 수정은 하지 않았다.
- 모델 synthetic tensor 로딩과, 기존 개발 노출 원본 BMLC002247 한 장을 사용하는 CPU 슬롯별 온라인 Notebook 확인을 먼저 한다. 확인용 실행은 본 회차 성적에 합산하지 않는다. 이번 모델로 OS 오프라인 검증을 마쳤다고 표시하지 않는다. 관리자 창·방화벽 변경은 없다.
- 본 평가: 1회 CPU 0–3, 2·4회 CPU 0,1,4,5, 3·5회 CPU 2,3,6,7. 각 4스레드. 같은 병행 묶음의 결과·입력·Notebook·모델 복사본은 서로 다른 폴더에 둔다.
- 각 Notebook 전체 시간(초기화·CSV 저장·종료 포함)을 측정한다. 목표 691.2/1600/1600/1260.8/1600초와 강제 종료 2400초를 구분한다. 프로세스 트리 메모리를 1초 간격으로 관측한다.
- 필드 득점/3N으로 채점하며 실패·누락은 분모 유지·무득점, NONE 일치는 득점한다. 전체 날짜 일치는 보조 지표다.

정확한 승인 원문·목록·코드·모델·정답·보호 파일 해시는 실행 경로의 `authorization.json`, `release.json`에 있다. 과거 최초 결과·승인·공용 weights·학습 결과를 덮어쓰지 않는다. 완료한 회차는 자동 재실행하지 않는다.

결과는 이 문서 옆 `epoch1_evaluation/round_01.md`~`round_05.md`와 `summary.md`에 작성한다. 미완료·실패는 로그와 부분 출력으로 그대로 남긴다. 2,110장 성적은 누적 개발 성적이며 독립 최종 성적이 아니다. 모델 채택·6회 진행·commit/push는 별도 사용자 결정 사항이다.
