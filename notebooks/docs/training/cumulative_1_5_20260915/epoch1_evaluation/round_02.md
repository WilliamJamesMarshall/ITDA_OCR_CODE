# 2회 1 epoch 후보 평가

원본 500장. 상태 `completed`. 필드 1281/1500 = 85.4000%. 95% 목표 미달.

완전 날짜 409/500는 보조 지표입니다. 실패 0건, 출력 누락 0건은 분모에 남고 득점하지 않습니다. NONE 일치는 득점합니다.

노트북 전체 1546.108초 / 목표 1600.0초. 목표 충족: True. 강제 종료 기준은 별도 2,400초입니다. 초기화·CSV 저장·종료를 포함하며 준비 복사와 사후 채점은 제외합니다.

CPU [0, 1, 4, 5]·4스레드. 프로세스 트리 RSS 관측 최대 1.337GiB, 관측 최소 시스템 여유 RAM 1.576GiB. 온라인 실행이며 OS 오프라인 검증으로 표시하지 않습니다.

오답 필드 원본 91장. 유형 집계(완전 날짜 기준): `{"미검출": 37, "날짜 불일치": 53, "오검출": 1}`. 원본 ID·정답·출력·오답 필드는 `C:\ITDA_OCR_WORKSPACE\grouped-6-originals-v1\cumulative-1-5-epoch1-eval-20260915-v1\evaluation\round_02\report.json`에 있습니다. 인식 부재와 선택 오류의 확정 분류에는 trace 검토가 필요하며, 채점 불일치만으로 원인을 단정하지 않습니다.

사용 checkpoint SHA `8ca6c7355fbdfbd12cd86d96898500e02a13f9d87c0ca2711c22d4eb89c00592`. 목록 SHA `230d10fd129a00f49c018dc3408a624edfc7ddab4ee7a46d5a202e315528822e`. 코드·모델·정답·승인 원문은 `C:\ITDA_OCR_WORKSPACE\grouped-6-originals-v1\cumulative-1-5-epoch1-eval-20260915-v1\release.json`에 고정했습니다.

누적 개발 성적이며 독립 최종 성적이 아닙니다. 기존 최초 결과·정답·기본 weights는 보존했습니다. 추가 학습·모델 채택·단계 종료·commit/push를 하지 않았습니다.

## 기존 채택 모델 대비 및 오답 검토

같은 최신 정답으로 기존 출력만 다시 계산한 기준 1287/1500 → 이번 1281/1500. 획득 27필드, 손실 33필드. 과거 성적·시간 판정은 덮어쓰지 않았습니다.

| 관측 유형 | 원본 수 | 오답 필드 | 코드·파이프라인 해결 방향 | 추가 학습 판단 |
|---|---:|---:|---|---|
| 정답 후보 미관측 | 42 | 106 | 검출·잘림·문자인식·파싱 중 실패 지점을 원본과 대조 | 인식 오류면 누적 학습 검토; 검출 누락은 인식기 학습만으로 해결 불가 |
| 정답 후보 관측·필드 보류 | 44 | 100 | 수용 임계값·부분 필드 보존·역할 충돌 경로 재현 | 후보가 이미 있어 추가 학습만의 우선순위는 낮음 |
| 정답 후보 관측·다른 값 선택 | 1 | 3 | 후보 순위·역할·날짜 순서 선택을 재현 | 표제 오인식이 확인되면 인식기 보완을 검토 |
| 정답 후보 미관측·순서/역할 문맥 경합 | 3 | 7 | 후보 생성과 순서/역할 해석을 분리 검토 | 인식 오류 증거가 있는 승인 crop만 후속 학습 후보 |
| NONE 정답에 날짜 필드 출력 | 1 | 3 | 제조일·로트·비날짜 역할과 후보 수용 조건 검토 | 표제 문자 인식이 부족한 경우에만 승인 crop 검토 |



이 유형은 저장 trace의 조사 경로이며 전 건의 구조적 원인을 확정한 시각 검증이 아닙니다. 정답 후보 존재만으로 코드 결함을 단정하지 않으며, 후보 미관측을 모두 학습 부족으로 분류하지 않습니다.

후속 원본 학습안: 오답 원본과 승인 crop 전사를 먼저 대조하고, 기존 optimizer/검증 상품 그룹 역할을 유지합니다. 검증 원본은 optimizer로 옮기지 않습니다. 저장 증강본은 제외하며, 누적 재학습은 새 사용자 피드백·승인과 별도 설정 검토 후에만 진행합니다. 이번 실행은 추가 학습을 수행하지 않았습니다.

이번 비교는 코드 개선과 1 epoch 학습이 함께 바뀐 누적 개발 평가이므로 각각의 효과를 분리한 실험은 아닙니다.

원본별 득실·후보 trace 위치·출력 근거: `C:\ITDA_OCR_WORKSPACE\grouped-6-originals-v1\cumulative-1-5-epoch1-eval-20260915-v1\evaluation\round_02\analysis.json`.
