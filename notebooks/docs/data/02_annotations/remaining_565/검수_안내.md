> 문서 열람용 사본. 계획 수정은 원본 `C:\ITDA_OCR_CODE\학습 및 테스트 결과\02_annotations\remaining_565\검수_안내.md`에서 수행한 뒤 이 도구로 다시 게시합니다. 승인/정답 데이터는 포함하지 않습니다.

# 보완 초안 565장 전용 검수

접속: http://127.0.0.1:8767

이번 대상은 `cohort.json`에 고정한 565장입니다. 다른 2,045장은 이 화면의 목록과 저장 대상에서 제외됩니다.

1. 사진 전체와 날짜 영역의 원문·역할을 확인합니다.
2. 맞으면 **승인 후 다음**을 누릅니다. 개별 확인 체크를 반복할 필요는 없습니다.
3. 틀린 부분만 원문/역할/꼭짓점을 수정한 뒤 **승인 후 다음**을 누릅니다.
4. 판단이 어려우면 보류하고 다음 사진으로 이동합니다.

사진별 최종 날짜 정답은 변경하지 않았습니다. 영역과 실제 인쇄 문자열은 AI 초안이며 아직 사람 승인 전입니다. 실제 반사·가려짐이 있는 글자는 정답 날짜로 추측해 채우지 않았습니다. 이 상태 자체는 학습 승인을 뜻하지 않습니다.

검증: 565장 모두 승인 경로의 구조 검사를 통과했습니다. 최종 날짜 변경 0건, 전체 자료의 원본 고정 해시 변경 0건, 외부 날짜 원문 404개 변경 0건입니다. HTTP 대상 제한 및 승인 저장을 포함한 관련 테스트 46개가 통과했습니다. 테스트 승인은 임시 자료에만 수행했습니다.

서버를 다시 시작할 때 프로젝트 폴더의 PowerShell에서 실행:

```powershell
.\.labeling_paddle_env\Scripts\python.exe -m scripts.ocr_annotations serve --port 8767 --cohort '학습 및 테스트 결과\02_annotations\remaining_565\cohort.json'
```

이전 초안은 `before` 폴더에, 수정 내역은 주석 이력에 보존되어 있습니다.
