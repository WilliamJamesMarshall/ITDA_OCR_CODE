# 미완료 기록

후속 작업 시작 시 판독 프로세스가 실행 중이지 않았고 runtime.json도 없었다. 일부 predictions.jsonl과 비어 있는 submission.csv만 존재하므로 완성된 전수 결과나 속도 측정값으로 사용하지 않는다.

현재 소스를 고정한 새 실행은 live_386_verified에 별도로 저장한다. 이 폴더의 원시 기록은 삭제하거나 덮어쓰지 않는다.
