# 중단된 개발 실행

초기 통합 후보의 기존 341건 고정 OCR 검사에서 6건 회귀를 발견하여 전수 실행을 중단했다. 이 디렉터리의 일부 판독값이나 실행 시간을 최종 성능으로 사용하지 않는다.

분리된 날짜 쌍의 문맥 연결과 시각 문자열의 날짜 오인식을 수정한 버전은 `live_386_final`에서 처음부터 다시 측정한다. 첫 중단 시도는 Python 실행기가 부모·자식 두 프로세스여서 안전 검사에서 멈췄으며, 프로세스 관계를 확인한 뒤 두 프로세스를 종료했다. 이 초기 기록은 완료된 벤치마크가 아니다.
