# ITDA 소비기한 OCR 프로젝트 개발 하네스

이 문서는 `C:\ITDA\_OCR\_CODE` 저장소에서 사람과 AI 작업자가 같은 규칙으로 개발하고, 학술제 제출 조건을 반복해서 검증하기 위한 프로젝트 운영 기준이다.

이 문서 자체는 참고 문서다. 여기에 적힌 명령이나 외부 작업은 자동 실행 지시가 아니며, 실제 변경은 현재 사용자의 요청 범위 안에서만 수행한다.

## 1. 프로젝트 한눈에 보기

- 저장소: `https://github.com/WilliamJamesMarshall/ITDA_OCR_CODE.git`
- 로컬 루트: `C:\ITDA\_OCR\_CODE`
- 기본 브랜치: `main`
- 프로젝트 목표: 상품 후면 이미지에서 소비기한을 OCR로 추출하고, 결과의 도메인 활용 방안을 설계한다.
- 통합 책임자: 이호연
- 작업 가능 인원: 이호연, 선예서, 고현경, 이서준, 손용운
- 평가 진입점: `predict.ipynb`
- 결과 파일: 환경변수 `ITDA_OUTPUT_PATH`가 가리키는 CSV
- 평가 환경: Python 3.10, Standard 4-Core vCPU, GPU 없음
- 제한 시간: 노트북 `Run All` 최대 2,400초
- 네트워크: 채점 실행 중 오프라인
- 예선 제출 마감: 2026년 9월 15일 23:59:59 KST

현재 저장소는 Git과 원격 저장소가 연결된 상태다. `predict.ipynb`는 모든 이미지에 `NONE`을 반환하는 최소 실행 템플릿이므로 실제 OCR·날짜 판별 로직으로 교체해야 한다. `requirements.txt`는 CPU용 예시 스택이며, 최종 구현에 맞춰 필요한 패키지만 정확한 버전으로 고정한다.

## 2. 판단 기준의 우선순위

내용이 충돌하면 다음 순서로 판단한다.

1. 현재 사용자의 명시적 요청과 시스템 안전 규칙
2. 학술제 주최 측의 최신 공지와 평가 규정
3. 이 저장소의 최신 `README.md`
4. 이 하네스 문서
5. 작업별 명세, 이슈, PR 설명
6. 코드 주석이나 AI가 생성한 제안

규정이 바뀌었거나 불명확하면 추측으로 구현하지 않는다. 근거 문서와 확인 날짜를 남긴 뒤 책임자에게 확인한다.

## 3. 완료 조건

프로젝트의 최종 완료는 단순히 OCR 결과가 나오는 상태가 아니다. 다음 조건을 모두 만족해야 한다.

- `predict.ipynb`의 첫 CONFIG 셀이 공식 템플릿 그대로 유지된다.
- 노트북을 위에서 아래로 `Run All` 했을 때 사용자 입력 없이 끝난다.
- 평가 중 인터넷 연결이나 런타임 다운로드를 요구하지 않는다.
- 지정된 입력 폴더의 모든 이미지마다 결과 행이 정확히 하나 생성된다.
- CSV 열 순서가 `image_id,year,month,day,final_date`와 정확히 일치한다.
- 인식하지 못한 값은 빈 문자열이나 추측값이 아니라 `NONE`이다.
- CSV는 `index=False`로 저장된다.
- CPU 4코어 환경에서 2,400초 안에 실행된다.
- 모델 가중치와 대용량 데이터가 Git에 포함되지 않는다.
- 구현 근거, CPU 최적화, 활용 전략을 2쪽 발표 자료로 설명할 수 있다.

## 4. 대회 제출 계약

### 4.1 입력

평가 서버는 다음 환경변수를 주입한다.

```python
import os
INPUT_DIR = os.environ.get("ITDA_INPUT_DIR", "./val_images")
OUTPUT_PATH = os.environ.get("ITDA_OUTPUT_PATH", "./submission.csv")
```

첫 코드 셀의 변수명, 환경변수명, 기본값을 임의로 바꾸지 않는다. 코드 전체가 `INPUT_DIR`와 `OUTPUT_PATH`를 기준으로 동작하게 한다. 로컬 절대 경로나 특정 사용자 폴더를 노트북에 하드코딩하지 않는다.

공식 데이터 기준으로 입력은 상품 후면 이미지 3,352장이다. 파일 확장자가 섞일 수 있으므로 특정 확장자 하나만 가정하지 않는다. `image_id`는 파일명의 확장자를 제거한 값 그대로 사용하고, 숫자형으로 변환해 앞자리 0을 잃지 않게 문자열로 유지한다.

### 4.2 출력

CSV 스키마는 다음과 같다.

| 열 | 규칙 |
|---|---|
| `image_id` | 입력 파일명에서 확장자만 제거한 문자열 |
| `year` | 판별한 연도 또는 `NONE` |
| `month` | 판별한 월 또는 `NONE` |
| `day` | 판별한 일 또는 `NONE` |
| `final_date` | 최종 소비기한 또는 `NONE` |

필수 검증:

- 열 이름과 순서가 정확하다.
- 입력 이미지 수와 출력 행 수가 같다.
- `image_id`가 중복되거나 누락되지 않는다.
- 인식 실패를 `NaN`, 빈칸, `null`, 임의 날짜로 기록하지 않는다.
- 저장 코드는 `df.to_csv(OUTPUT_PATH, index=False)` 형태를 유지한다.

날짜 문자열의 세부 형식은 최신 공식 규정과 제공 예시를 우선한다. 규정에 없는 형식을 임의로 정규화하지 않는다.

### 4.3 오프라인 모델과 가중치

채점 서버의 노트북 실행 단계에는 인터넷이 없다.

- 사전학습 오픈소스 모델은 사용할 수 있다.
- 유료 외부 LLM/VLM API와 추론 중 사람의 개입은 사용할 수 없다.
- 모든 가중치는 평가 실행 전에 로컬 `weights/`에 준비되어 있어야 한다.
- `predict.ipynb` 안에서 모델, 패키지, 사전 파일을 다운로드하지 않는다.
- 라이브러리가 자동 다운로드를 시도하지 않도록 명시적으로 비활성화한다.
- 가중치 준비가 필요하면 선택적으로 `download_weights.sh`를 추가할 수 있다. 운영자가 평가 전에 한 번 실행하며, 노트북은 이 파일에 의존해 네트워크 요청을 해서는 안 된다.

예시:

```python
reader = easyocr.Reader(
    ["en"],
    gpu=False,
    model_storage_directory="./weights",
    download_enabled=False,
)
```

패키지 설치 시간과 사전 가중치 다운로드 시간은 속도 점수에 포함되지 않는다. 점수에 포함되는 것은 준비가 끝난 뒤 `predict.ipynb`를 `Run All` 하는 시간이다.

## 5. 저장소와 파일 경계

공식 템플릿의 최소 구조를 보존한다.

```text
C:\ITDA\_OCR\_CODE\
├─ README.md
├─ Harness_README.md
├─ predict.ipynb
├─ requirements.txt
├─ .gitignore
├─ download_weights.sh      # 필요할 때만 추가
├─ src\                     # 코드가 커질 때만 추가
├─ tests\                   # 재사용 가능한 로직을 분리할 때 추가
└─ weights\                 # 로컬 전용, Git 제외
```

`src/`와 `tests/`는 의무가 아니다. 노트북이 작고 명확하면 불필요한 추상화를 만들지 않는다. 다만 전처리, OCR, 날짜 파싱이 반복되거나 검증이 어려워지면 다음 정도로만 분리한다.

```text
src/
├─ io.py             # 입력 탐색과 이미지 식별자
├─ preprocess.py     # 회전, 크롭, 명암 보정
├─ ocr.py            # OCR 추론
└─ date_parser.py    # 후보 추출, 검증, 최종 날짜 선택
```

다음 파일은 커밋하지 않는다.

- 원본·가공 이미지와 라벨 데이터
- `*.zip`
- `submission.csv` 등 실행 결과물
- `*.pt`, `*.pth`, `*.onnx`, `*.safetensors`, `*.bin`
- `weights/` 아래 실제 가중치
- 가상환경, 캐시, 노트북 체크포인트

현재 `.gitignore` 규칙을 약화하지 않는다. 작은 재현용 fixture가 정말 필요하면 개인정보·저작권·대회 규정을 확인하고 별도 승인을 받아 최소 파일만 추가한다.

## 6. 팀 작업 규칙

### 6.1 작업자 설정

향후 협업 세션 관리 도구를 설치해 `.git-session.json`을 사용한다면 기준값은 다음과 같다. 현재 파일이나 도구가 존재한다고 가정하지 않는다.

```json
{
  "integrationOwner": "이호연",
  "allowedWorkers": [
    "이호연",
    "선예서",
    "고현경",
    "이서준",
    "손용운"
  ]
}
```

목록에 없는 이름을 자동으로 추가하지 않는다. 변경이 필요하면 이 문서와 실제 협업 설정을 같은 PR에서 함께 갱신한다.

### 6.2 한 작업의 범위

하나의 작업은 하나의 검증 가능한 목표만 가진다.

좋은 예:

- 이미지 방향 보정 후보를 추가하고 샘플 실행 시간을 비교한다.
- OCR 문자열에서 `YYYY.MM.DD` 후보를 추출하는 파서를 구현한다.
- 결과 CSV의 스키마와 누락을 검사하는 검증 코드를 추가한다.

피해야 할 예:

- OCR 모델 교체, 폴더 구조 개편, 문서 전면 수정, 성능 튜닝을 한 번에 처리한다.
- 근거 없이 여러 모델과 프레임워크를 동시에 추가한다.
- 현재 작업과 무관한 파일을 정리하거나 포맷팅한다.

작업 시작 전에 아래 네 줄을 남긴다.

```text
목표:
수정 파일:
성공 기준:
하지 않을 일:
```

### 6.3 브랜치와 GitHub Desktop

공유 `main`에서 직접 개발하지 않는다. 브랜치 이름은 충돌을 줄일 수 있게 작업자와 목적을 포함한다.

```text
feature/<worker>/<short-purpose>
fix/<worker>/<short-purpose>
docs/<worker>/<short-purpose>
```

예: `feature/hoyeon/date-parser`

GitHub Desktop 작업 순서:

1. `C:\ITDA\_OCR\_CODE` 저장소를 선택하고 `Fetch origin`을 실행한다.
2. `main`을 최신 상태로 맞춘다.
3. 작업 전용 브랜치를 만든다.
4. 작업 파일만 변경하고 Changes 목록에서 의도하지 않은 파일이 없는지 확인한다.
5. 검증 결과를 확인한 뒤 의미가 드러나는 메시지로 커밋한다.
6. `Push origin`을 실행하고 GitHub에서 Draft PR을 만든다.
7. 통합 책임자 검토와 필수 검증이 끝난 뒤에만 병합한다.

커밋 메시지 예:

```text
feat: add date candidate parser
fix: preserve leading zeros in image ids
test: validate submission schema
docs: align harness with offline evaluation
```

다른 작업자의 브랜치나 커밋을 강제로 덮어쓰지 않는다. 충돌이 생기면 양쪽 의도를 확인하고 작은 단위로 해결한다.

## 7. OCR 파이프라인 기준

기본 파이프라인은 단순하고 측정 가능하게 유지한다.

```text
입력 탐색
→ 이미지 로드와 방향 확인
→ 필요한 최소 전처리
→ CPU OCR
→ 날짜 후보 추출
→ 형식·범위·문맥 검증
→ 최종 후보 선택 또는 NONE
→ CSV 계약 검증과 저장
```

### 7.1 전처리

전처리는 정확도 향상이 측정될 때만 추가한다.

- 원본, 회전, 제한된 리사이즈·크롭 후보부터 시작한다.
- 모든 이미지에 무거운 조합을 무조건 적용하지 않는다.
- 저비용 1차 시도에서 신뢰할 만한 날짜가 나오면 조기 종료한다.
- 해상도와 후보 수에 상한을 둔다.
- 오류가 나도 전체 실행이 멈추지 않도록 이미지 단위로 처리한다.

### 7.2 OCR

- `gpu=False`를 명시한다.
- 모델은 프로세스 또는 노트북 실행 중 한 번만 로드한다.
- CPU 스레드 수를 4코어 평가 환경에 맞춘다.
- 배치가 실제로 빨라지는지 측정하기 전에는 복잡한 병렬화를 추가하지 않는다.
- 한글·영문·숫자 언어 설정은 샘플 근거로 결정한다.

### 7.3 날짜 후보와 최종 선택

후보 파서는 구분자와 OCR 혼동을 견디되, 존재하지 않는 날짜를 만들어 내지 않는다.

- `YYYY-MM-DD`, `YYYY.MM.DD`, `YYYY/MM/DD` 같은 명시적 날짜를 우선한다.
- `YYMMDD`, `YYYYMMDD` 등 붙은 숫자는 문맥과 길이를 함께 확인한다.
- `O/0`, `I/1`, `S/5` 치환은 숫자 위치에서만 제한적으로 적용한다.
- 월 1~12, 실제 일수, 윤년을 검증한다.
- 제조일, 포장일, 로트번호와 소비기한 문맥을 구분한다.
- 근거가 약하거나 후보가 충돌하면 억지로 채우지 않고 `NONE`을 선택한다.

후보 점수 규칙을 사용한다면 항목과 가중치를 코드와 문서에 명시한다. 예를 들어 OCR 신뢰도, 날짜 형식 완결성, 소비기한 키워드 거리, 캘린더 유효성, 비현실적 연도 패널티를 독립적으로 기록한다.

## 8. 실험과 품질 관리

### 8.1 골든 샘플

전체 정답이 제공되지 않더라도 팀 내부에서 대표 샘플을 만든다.

- 정면·회전·기울어진 이미지
- 저조도·반사·흐림·저해상도
- 날짜가 여러 개인 제품
- 한글, 영문, 숫자가 섞인 인쇄
- 점자형·잉크젯형·라벨형 날짜
- 날짜가 없거나 읽을 수 없는 이미지

라벨을 새로 만들 때는 원본과 분리하고, 작성자·작성일·판단 근거를 남긴다. 추가 데이터 수집과 라벨링은 예선 가산점 대상이 될 수 있지만, 라이선스와 개인정보를 먼저 확인한다.

### 8.2 오류 분류

실험 결과는 최소 다음 범주로 나눈다.

- 이미지 탐색·로드 실패
- 회전 또는 관심영역 실패
- OCR 미검출
- 문자 인식 오류
- 날짜 후보 추출 실패
- 제조일/소비기한 문맥 혼동
- 복수 후보 선택 오류
- CSV 계약 오류
- 시간 또는 메모리 초과

전체 정확도 하나만 보지 말고 범주별 실패 수와 대표 샘플을 기록한다.

### 8.3 변경 승인 기준

모델이나 전처리 변경은 다음 표를 채울 수 있을 때만 채택한다.

| 항목 | 기준선 | 변경 후 | 판정 |
|---|---:|---:|---|
| 골든 샘플 정확도 |  |  |  |
| 날짜 완전 일치 수 |  |  |  |
| `NONE` 오판 수 |  |  |  |
| 평균 초/이미지 |  |  |  |
| 전체 예상 시간 |  |  |  |
| 최대 메모리 |  |  |  |

정확도 이득 없이 실행 시간이나 복잡도만 늘면 채택하지 않는다. 실험용 코드와 실제 제출 경로를 섞지 않는다.

## 9. 검증 절차

### 9.1 빠른 정적 점검

PowerShell에서 실행:

```powershell
Set-Location -LiteralPath 'C:\ITDA\_OCR\_CODE'
git status --short --branch
git diff --check
python -m pip check
```

`python -m pip check`는 의존성이 설치된 개발 환경에서만 실행한다. 패키지 설치는 별도 작업이며 문서 검증을 위해 자동으로 수행하지 않는다.

### 9.2 제출 형식 점검

결과가 생성된 뒤 다음 조건을 검사한다.

```python
from pathlib import Path
import pandas as pd

input_dir = Path("val_images")
output_path = Path("submission.csv")
expected_columns = ["image_id", "year", "month", "day", "final_date"]

image_ids = {p.stem for p in input_dir.iterdir() if p.is_file()}
df = pd.read_csv(output_path, dtype=str, keep_default_na=False)

assert list(df.columns) == expected_columns
assert len(df) == len(image_ids)
assert df["image_id"].is_unique
assert set(df["image_id"]) == image_ids
assert not df.isna().any().any()
assert (df[expected_columns[1:]] != "").all().all()
```

이 코드는 검증 예시다. 최종 날짜 표현 규칙 검사는 최신 공식 형식에 맞춰 추가한다.

### 9.3 공식 방식의 노트북 실행

공식 README와 같은 Linux 명령은 다음과 같다.

```bash
ITDA_INPUT_DIR=/path/to/val_images \
ITDA_OUTPUT_PATH=/path/to/submission.csv \
jupyter nbconvert --to notebook --execute predict.ipynb \
  --ExecutePreprocessor.timeout=2400 \
  --output /tmp/predict_executed.ipynb
```

Windows 개발 환경에서는 동등한 환경변수를 주입해 실행하되, 최종 제출 전에는 Python 3.10, CPU 4코어, GPU 비활성, 네트워크 차단 조건에서 다시 검증한다. 테스트 출력 노트북과 CSV는 커밋하지 않는다.

### 9.4 최종 오프라인 리허설

1. 깨끗한 환경에 `requirements.txt`를 설치한다.
2. 필요하면 운영 단계에서 `download_weights.sh`를 한 번 실행한다.
3. 네트워크를 차단한다.
4. 별도 입력·출력 경로를 환경변수로 주입한다.
5. `predict.ipynb`를 처음부터 끝까지 실행한다.
6. 시간, 오류, 행 수, 스키마, `image_id` 집합을 기록한다.
7. 같은 조건에서 다시 실행해 재현성을 확인한다.

## 10. PR 하네스

Draft PR 설명은 다음 형식을 사용한다.

```markdown
## 목표

## 변경 파일과 이유

## 하지 않은 일

## 검증
- [ ] 작은 샘플 Run All
- [ ] CSV 열 순서와 행 수
- [ ] 오프라인 실행
- [ ] CPU 4코어 실행 시간
- [ ] `git diff --check`

## 측정 결과
- 정확도 또는 성공 샘플:
- 평균 초/이미지:
- 전체 예상 시간:
- 최대 메모리:

## 위험과 되돌리기

## 리뷰 요청 사항
```

리뷰어는 다음을 우선 확인한다.

- 변경이 작업 목표에 직접 필요한가.
- 공식 CONFIG 셀과 출력 계약을 깨지 않는가.
- 인터넷·GPU·개인 로컬 경로에 숨어서 의존하지 않는가.
- 새 라이브러리와 가중치가 꼭 필요한가.
- 정확도·속도 변화가 측정됐는가.
- `NONE` 처리와 날짜 유효성 검사가 보수적인가.
- 데이터나 가중치가 Git 변경 목록에 섞이지 않았는가.

## 11. 발표 자료와 구현의 연결

예선 평가는 정확도뿐 아니라 속도, 구조 논리, 최적화·활용 전략을 함께 본다. 구현하면서 다음 근거를 보존한다.

| 평가 항목 | 배점 | 하네스에서 남길 근거 |
|---|---:|---|
| 정확도 | 50 | 골든 샘플, 오류 분류, 최종 CSV 검증 |
| 속도 | 10 | CPU 4코어 `Run All` 실측 시간 |
| 아키텍처 논리 | 15 | 파이프라인, 후보 선택 규칙, 실패 처리 |
| 최적화·활용 전략 | 25 | 조기 종료, 비용 대비 효과, 도메인 활용안 |
| 추가 데이터 수집·라벨링 | 최대 5 가산점 | 출처, 라이선스, 라벨 기준과 품질 기록 |

### 1쪽: 파이프라인

- 입력부터 CSV까지의 단계
- 사용 OCR 모델과 선택 근거
- 전처리 및 날짜 후보 선택 규칙
- 실패 시 `NONE`으로 가는 조건

### 2쪽: 근거와 전략

- CPU 4코어 최적화와 실제 시간 측정
- 정확도와 속도의 절충
- 대표 실패 유형과 개선 방향
- 소비기한 OCR의 도메인 활용 시나리오
- 추가 데이터 수집·라벨링을 했다면 범위와 품질 관리

발표 자료의 수치와 최종 코드 측정값이 다르면 안 된다. 마지막 리허설 결과로 표와 다이어그램을 갱신한다.

## 12. 릴리스 체크리스트

### 저장소

- [ ] 원격 저장소가 올바른 URL을 가리킨다.
- [ ] 제출 브랜치와 `main`의 관계가 명확하다.
- [ ] 저장소가 Public이거나, Private이면 마감 전에 `b9511242000-blip` 계정에 collaborator 권한을 부여했다.
- [ ] 데이터, 결과물, 가중치, 비밀값이 커밋되지 않았다.
- [ ] `requirements.txt`의 실제 의존성이 `==`로 고정됐다.
- [ ] 필요한 경우에만 `download_weights.sh`가 있으며 한 번에 성공한다.

### 실행

- [ ] 첫 CONFIG 셀이 공식 템플릿과 같다.
- [ ] 사용자 입력과 수동 클릭이 없다.
- [ ] 평가 실행 중 네트워크 요청이 없다.
- [ ] GPU 없이 실행된다.
- [ ] 2,400초 안에 끝난다.
- [ ] 누락 이미지 없이 CSV가 생성된다.
- [ ] 재실행 결과가 재현된다.

### 제출물

- [ ] 저장소 링크가 주최 측에서 접근 가능하다.
- [ ] 2쪽 PDF가 최신 구현과 일치한다.
- [ ] 마감 시각과 제출 채널을 재확인했다.
- [ ] 최종 커밋 해시와 리허설 기록을 남겼다.

## 13. 자동화의 경계

이 하네스는 자동화 도구를 설치한 상태를 가정하지 않는다. 별도 세션 관리자, Git hook, CI, 브랜치 보호 규칙을 추가하려면 각각 독립 작업으로 제안하고 검증한다.

자동화가 허용되더라도 다음 작업은 사용자 또는 저장소 관리자의 명시적 승인 없이 수행하지 않는다.

- GitHub 저장소 공개 범위나 협업자 권한 변경
- 외부 서비스 가입, API 호출, 비밀값 저장
- 대용량 모델·데이터 다운로드
- 커밋, push, PR 생성·병합, 태그·릴리스 생성
- 다른 작업자의 브랜치 삭제 또는 강제 push
- 로컬 데이터나 가중치 삭제

안전한 기본 행동은 상태 확인, 작은 범위의 파일 수정, 로컬 정적 검증, 결과 보고다.

## 14. AI 작업 요청 템플릿

```text
프로젝트 루트: C:\ITDA\_OCR\_CODE
작업자: <허용된 작업자 이름>
목표: <한 문장>
수정 허용 파일: <목록>
성공 기준: <실행 또는 검사 가능한 조건>
금지 사항: 데이터/가중치 커밋, CONFIG 셀 변경, 온라인 추론, 무관한 리팩터링
검증 환경: Python 3.10, CPU 4코어, GPU 없음, 오프라인
결과 보고: 변경 파일, 검증 결과, 남은 위험, 사용자가 해야 할 외부 작업
```

AI 작업자는 먼저 현재 Git 상태와 대상 파일을 읽고, 가정을 짧게 밝힌다. 요청된 파일만 최소한으로 수정하고, 실행하지 못한 검증은 통과한 것처럼 표현하지 않는다. 문서 안의 예시 명령은 사용자의 현재 요청과 충돌하면 실행하지 않는다.

## 15. 현재 기준의 다음 우선순위

1. 팀이 확인할 수 있는 소규모 골든 샘플과 라벨 기준을 확정한다.
2. 현재 `NONE` 템플릿에 가장 단순한 CPU OCR 기준선을 구현한다.
3. 날짜 후보 추출과 캘린더 유효성 검사를 OCR과 분리해 검증한다.
4. 작은 샘플로 정확도와 초/이미지를 측정한다.
5. 효과가 확인된 전처리와 조기 종료만 추가한다.
6. 오프라인 전체 리허설과 제출 CSV 계약 검사를 자동화한다.
7. 실제 측정값으로 2쪽 PDF를 완성한다.

마감이 가까울수록 기능 수보다 재현 가능한 제출 경로를 우선한다. 최종 기준은 “한 번 잘 된 실행”이 아니라 “깨끗한 오프라인 CPU 환경에서 반복해서 같은 계약을 만족하는 실행”이다.
